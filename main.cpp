#include <iostream>
using namespace std;

int main(){
    float x;
    float y;
    float q;
    cout << "Enter the number(x): ";
    cin >> x;
    cout << "Enter the number(y): ";
    cin >> y;
    cout << "Enter the number(q): ";
    cin >> q;
    cout << "Sum: " << (x+y+q) << endl;
    cout << "Multiplication: " << (x*y*q) << endl;
    return 0;
}